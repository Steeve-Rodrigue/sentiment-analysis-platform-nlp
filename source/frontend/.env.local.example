API_URL=http://localhost:8000

# Only needed when running via docker-compose: overrides the base URL
# used by server-rendered pages (e.g. /models) so they reach the
# backend over the Docker network instead of NEXT_PUBLIC_API_BASE_URL.
# API_BASE_URL_INTERNAL=http://backend:8000
